name: dynamic_themed_app_flutter
description: A new Flutter dynamic themed app.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  flutter_bloc: ^3.1.0
  equatable: ^1.0.2

  json_serializable: ^3.2.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.0.0
  json_serializable: ^3.2.0


flutter:
  uses-material-design: true

  assets:
    - assets/